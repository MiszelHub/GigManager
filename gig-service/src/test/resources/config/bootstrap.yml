spring:
    cloud:
        consul:
            discovery:
                enabled: false
                instanceId: ${spring.application.bandName}:${spring.application.instance-id:${random.value}}
            config:
                enabled: false
